The DUX working state — shown while a query runs. A card with a sweeping brand-gradient bar, a bobbing brand mark, and a shimmering headline.

```jsx
<ThinkingIndicator head="Reading your operations data…" />
```

Customize `head` / `sub`. Renders its own keyframes; place it where the assistant reply will appear, then swap it for the result.
