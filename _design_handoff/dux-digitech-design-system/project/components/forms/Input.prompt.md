Single-line text field. Hairline border that lifts to an iris focus ring.

```jsx
<Input icon={<Icon name="search" size={16} />} placeholder="Search suppliers…" />
```

Pass any element as `icon`. Controlled via `value` / `onChange`.
