Generic count / label chip. For workflow states use `StatusTag`; for query filters use `FilterPill`.

```jsx
<Badge tone="iris">Beta</Badge>
<Badge tone="cyan" mono>24</Badge>
```

Tones: neutral, iris, cyan, ok, pending, err. Set `mono` for tabular numerics.
