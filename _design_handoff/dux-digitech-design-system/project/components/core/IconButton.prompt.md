Square icon-only control for headers and toolbars. `send` variant is the iris-gradient composer button.

```jsx
<IconButton icon="moon" title="Toggle theme" />
<IconButton icon="send" variant="send" size={40} title="Send" />
```

Default hovers to iris border + color. Use `ghost` for borderless toolbar actions.
