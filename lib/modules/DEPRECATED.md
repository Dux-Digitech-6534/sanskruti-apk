Deprecated legacy module tree.

The active application uses `lib/features`, `lib/repositories`, and
`lib/routes/app_router.dart`.

Do not add new code here. Keep this folder only as a historical reference until
it can be safely deleted.
